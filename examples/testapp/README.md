# DRF Registration Example

## Installing

At `testapp` root directory run `pip install -r requirements.txt` to install packages

## Start server

Run `python manage.py runserver`.

Access at http://localhost:8000/
